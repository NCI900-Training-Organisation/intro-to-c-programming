/* Retional operators in C is much similar to Python*/

#include <stdio.h>

void main()
{
    int a = 7, b = 2, c = 2;
    
    printf("Is a < b? %d \n", (a < b));
    printf("Is a > b? %d \n", (a > b));
    printf("Is b >= c? %d \n", (b >= c));
    printf("Is b <= c? %d \n", (b <= c));
    printf("Is b == c? %d \n", (b == c));
    printf("Is b !=c? %d \n", (b != c));
}